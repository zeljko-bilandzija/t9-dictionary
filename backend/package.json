{
    "name": "backend",
    "version": "1.0.0",
    "description": "Task for Kiwi.com",
    "main": "index.js",
    "engines": {
        "node": "10.0.0"
    },
    "scripts": {
        "test": "cross-env NODE_ENV=test && mocha server/**/*.test.js",
        "test-watch": "nodemon --exec \"npm test\"",
        "start": "node server/server",
        "start-watch": "nodemon server/server"
    },
    "author": "Zeljko Bilandzija",
    "license": "ISC",
    "dependencies": {
        "express": "^4.16.3"
    },
    "devDependencies": {
        "cross-env": "^5.2.0",
        "expect": "^23.4.0",
        "mocha": "^5.2.0",
        "nodemon": "^1.18.3",
        "supertest": "^3.1.0"
    }
}